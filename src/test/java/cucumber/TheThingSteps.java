package cucumber;

import io.cucumber.java8.En;

public class TheThingSteps implements En {
    public TheThingSteps() {
        Given("Adeel wants to start running cucumber based tests", () -> {
        });

        When("we add the dependencies", () -> {
        });

        When("set up the necessary files", () -> {
        });

        Then("all will be good and swell", () -> {
        });
    }
}
